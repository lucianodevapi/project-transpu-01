

server:
  port: 8001

spring:
  application:
    name: ms-company

  datasource:
    url: jdbc:postgresql://localhost:5432/transpu-company-db
    password: postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true